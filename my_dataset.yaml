# 数据集根目录（绝对路径）
path: D:/DataSet/DroneVehicle_Processed

# 训练集和验证集路径
train:
  - images/train/rgb  # 可见光训练集
  - images/train/ir   # 红外训练集
val:
  - images/val/rgb    # 可见光验证集
  - images/val/ir     # 红外验证集

# 类别配置（必须用列表，顺序对应类别ID 0-4）
nc: 5
names: ['car', 'truck', 'bus', 'van', 'Freight Car']